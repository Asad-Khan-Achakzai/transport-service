import { CnicMaskDirective } from './cnic-mask.directive';

describe('CnicMaskDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new CnicMaskDirective();
  //   expect(directive).toBeTruthy();
  // });
});
